export default Object.freeze({
    TILE_DEFAULT_SIZE: '50px', // Taille des tuiles par défaut
    WAVE_DELAI: 5, // Delai en secondes avant de passer à la vague suivante
    MONSTER_DELAI: .5, // Delai en secondes avant de passer au monstre suivant
});
